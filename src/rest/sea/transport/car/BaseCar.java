package rest.sea.transport.car;

import rest.sea.transport.BaseTransport;

public abstract class BaseCar extends BaseTransport {

    public BaseCar(String name, int speed) {
        super(name, speed);
    }
}
