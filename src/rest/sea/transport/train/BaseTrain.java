package rest.sea.transport.train;

import rest.sea.transport.BaseTransport;

public abstract class BaseTrain extends BaseTransport {

    public BaseTrain(String name, int speed) {
        super(name, speed);
    }
}
